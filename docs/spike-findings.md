# mkt016 Spike Findings

Running record of the S1–S8 verification spike for **mkt016 — From Question to Campaign**. Plan reference: `mkt016-plan.md` §4 (spike), §5 (task paths), §7 (provisioning), §10 (open questions).

Started: 2026-09-18
Project(s) used: `qwiklabs-gcp-04-df5f1f023984` (a real Qwiklabs lab project, so the lab-project results below are the real ones)

Status key: **PASS** (plan path works as written) · **PARTIAL** (works with a change the lab must absorb) · **FAIL** (fallback path engaged) · _pending_

## Summary: what the spike locks

| ID | Status | Locks |
| :-- | :-- | :-- |
| S1 | **PASS** | Task 0 as written; the admin Add-agent dialog offers both A2A and Agent Runtime, so S5 and Task 6 have both registration routes |
| S2 | **PASS** (with lab-text changes and one data fix) | Tasks 1–2 primary path: BigQuery data agent → A2A card → Gemini Enterprise. Q2 closed for the end-user side. Agent Registry route is out (needs an Agent Gateway). |
| S3 | **PARTIAL** | Skills work, but can't be used with an agent ("Using agents and skills together is not supported yet"). The readout needs a manual copy → new chat → `/executive-readout` hand-off. Task 2's Builder-leg route (Skill with hand-off, the verified-query fallback, or the readout format in the data agent's instructions) is a planning call. |
| S4 | **PASS** | Task 3 primary path: MCP Toolbox for Databases on Cloud Run as a custom MCP data store, with both tools as plain `bigquery-sql` (no FastMCP server needed). Read-only tool runs without a prompt; the write tool shows a Review card; retries return the same receipt. **Q1 closed:** no org-policy error or override, and no FQDN allow-listing, in a Qwiklabs project. | Task 3 primary path (custom MCP data store); Q1 |
| S5 | **PASS** | Task 6: an ADK agent reaches Gemini Enterprise both ways. `orchestrator` (Cloud Run, A2A card) and `orchestrator2` (Agent Runtime) are both registered and appear in the assistant's `@` list. Both deploy in under 3 min (Agent Runtime 2m42s, Cloud Run 2m25s), so **R1 is closed** and either host fits Start Lab. Both need `GOOGLE_CLOUD_LOCATION=global`. Task 3 fallback not needed (S4). |
| S6 | **PASS** (Patrick's call; open items below) | Tasks 4–5: a Gemini Enterprise agent grounded on the brand corpus data store writes an on-voice brief from the corpus and takes follow-ups; `@`-invocation works. Agent type (Chat agent recommended over Workflow) is a planning call (Contradiction 17). |
| S7 | **PASS** (via Chat agent) | The assistant and a **Chat agent** (new Brand Studio agent) both generate images; the **Workflow agent errors**. Task 5's creative leg stays inside Brand Studio *if Brand Studio is a Chat agent*, which settles Contradiction 17. No image tool needed. |
| S8 | **PASS (tight)** | 12 PDFs, Layout Parser, direct from `gs://class-demo/…/v1/brand_corpus/`: **~10 min** import. Fits the ≤15-min Start Lab budget only if the import fires early and runs in parallel with the BigQuery load, and Task 4 isn't reached before it finishes. |

## Hand-back to planning (end of Phase 1, Sep 18)

**Paths the spike locks**
- **Task 0:** activate Gemini Enterprise, then Configurations → Feature management → Enable chat agents, Enable workflow, Enable skills (+ model selector, canvas, Gemini 3.8 Flash); allow a short propagation delay. **Also:** Settings → Authentication → identity provider = **Google Identity** (the custom MCP data store can't be created without it), unless SC3 can set it.
- **Tasks 1–2:** a BigQuery **data agent** (BigQuery → Agents → New agent, 9 tables) → publish twice → **Copy JSON** → Gemini Enterprise admin → Agents → + Add agent → **Custom agent via A2A**. OAuth is handled by publishing; users click **Authorize** once. No ADK analyst fallback.
- **Task 2 readout:** the assistant **Skill** `executive-readout` (paste three fields; `skills/executive-readout/SKILL.md`). Skills can't run with an agent: copy the Analyst's summary → skill page → **New chat** (pre-fills the trigger) → paste.
- **Task 3:** **MCP Toolbox for Databases** on Cloud Run as a **custom MCP data store** (auth None, no org-policy override). `resolve_segment` (read-only, no prompt) and `activate_segment` (**Review: Activate Segment** card; idempotent receipt). No FastMCP server.
- **Tasks 4–5:** Brand Studio as a **Chat agent** (not a Workflow), grounded on the **Cymbal Voyages Brand Corpus** data store (Layout Parser, direct from `class-demo`), with the Toolbox data store attached for `variant_performance`. It generates images itself.
- **Task 6:** the ADK orchestrator via A2A; both Cloud Run (2m25s) and Agent Runtime (2m42s) work; the companion leans Cloud Run.

**Decisions planning needs to make:** Task 2 readout route (Contradiction 10); how much glossary and verified-query text students type versus what is pre-loaded (5, 11); the Task 3 "read the schemas" beat, which the Review card or `tools.yaml` could carry (16); orchestrator host (S5); the Task 1 bare-assistant risk if the corpus is connected at Start Lab (6); the new Task 1 contrast beat (9).

**Hand-offs already written:** `docs/sc1-fix-prompt.md` (done, commit `d7c1734`), `docs/sc1-fix-prompt-2.md` (verified queries; not yet run). SC3 items: 3, 8, 13, 14, 18, 19.

---

## Contradictions to take back to planning

1. **S1 — D8 / Task 0 wording.** Workflows and agents are two separate toggles and two separate object types in the app (**Enable chat agents** and **Enable workflow**), not one renamed feature. Task 0 should name the exact toggles (list under S1). Agents built both ways show up in the agents table as *Employee-made*.
2. **S1 — who registers custom agents.** Custom agents (A2A, Agent Runtime) are registered only from the **admin console** (Gemini Enterprise → Agents → **+ Add agent**). The end-user app has no way to do it. Every "register by card" step (Tasks 1, 3 fallback, 6) is an admin-console step for the Builder, not something done in the web app.
3. **S1 — SC3 provisioning note.** `data/sql/load.sh` was committed without its execute bit (git mode 100644), so it failed to run until `chmod +x`. Fixed in the repo (mode 100755, staged, not yet committed). The copy in GCS will not keep the bit either, so provisioning should call `bash load.sh` rather than `./load.sh`.
4. **S2 — the data gives the answer away (SC1 fix needed).** With **no** instructions, glossary or verified queries, the data agent's first answer was the full root cause: −701 (−18.1%), cold markets −663 (−33.5%), *"because the marketing budget for the Warm Escapes Retargeting campaign (CMP-002…) was reallocated on July 24, 2026, to fund … Fall City Breaks 2026 (CMP-012)."* The source is the `ad_performance` **table description** that `load.sh` applies. It says the allocation changed on Jul 24. That collapses Task 2's "follow the thread", and it weakens Task 1's "the definitions are what matter" beat, because the agent does brilliantly without them. **Suggested fix (SC1):** in `data/cvgen/schemas.py`, `data/schemas/_tables.json` and `data/docs/data-dictionary.md`, find `Total paid spend is steady across the summer of 2026, but its allocation between campaigns changed on Jul 24, 2026.` and delete that sentence, leaving `Daily paid-media results by campaign and market, Jul 1, 2025 – Sep 15, 2026. Only paid channels (paid_search, paid_social, affiliate) appear here; email and organic have no media spend.` Then re-stage and re-run the unaided question. A second leak of the same kind: the CMP-002 `retrospective` in `data/content/campaigns.json` ends `Budget was rotated to Fall City Breaks in late July 2026 to fund the launch.` Delete it too. The full fix prompt for SC1 is in `docs/sc1-fix-prompt.md`. **Resolved Sep 18:** SC1 applied the fix (commit `d7c1734`). It was reloaded into the spike project, and the `ad_performance` description was set by hand with `bq update` because the Cloud Shell clone was stale (see note 8). The re-test is under S2, "Re-test after the data fix". Also consider whether the `retargeting` glossary entry, which names CMP-002 and `lapsed_compass_cold`, gives too much away once the Builder types it in Task 1.
5. **S2 — Task 1 Builder leg timing.** Patrick found typing the glossary terms and the verified query "time-consuming". The 14-minute Builder leg as drafted (8 glossary terms, 3 verified queries in `data-dictionary.md`) is at risk. The lab should give copy-paste blocks and probably a shorter set (e.g. 3–4 terms, 1 verified query). This is a planning call.
6. **S2 → planning: can the bare assistant in Task 1 see the answer?** `brand_corpus/05-brief-fall-city-breaks-2026.md` says outright that CMP-012 was funded by rotating CMP-002's budget from July 24. That is fine for Brand Studio. But §7 creates the corpus data store at Start Lab, so if the assistant grounds on connected data stores by default, Task 1's "confident, generic, wrong" beat could come back half-right. Check in S6/S8 whether the assistant cites the corpus before the Builder attaches it.
7. **S2 — Knowledge Catalog API.** The checklist's "Knowledge Catalog" API was not in the API list. Everything worked without it, so it is not a requirement for this path. It may be listed under its older service name (`dataplex.googleapis.com`). Check before SC3 puts it in the API list.
8. **S2 → SC3: `load.sh` prefers a local `schemas/` folder over the bucket's.** The first reload after the fix still applied the old description, because it ran from a Cloud Shell clone taken before the fix, and `load.sh` uses `../schemas` when that folder exists. Provisioning must run `load.sh` from a fresh checkout at a pinned commit, or from a copy with no local `schemas/` so it pulls from `gs://…/v1/schemas/`. Otherwise bucket and descriptions can silently disagree.
9. **S2 → planning: a new Task 1 beat is available.** After the fix, the unaided agent confidently blames the **Aug 1 price increase** (a red herring worth about 18 bookings, 3% of the gap) alongside the cold-market miss and low retargeting spend. The instructed agent decomposes instead of asserting a cause. "Without the definitions it sounds sure and is partly wrong" is a stronger Task 1 contrast than "without the definitions it's vague".
10. **S3 — a Skill can't hand off to (or run alongside) the data agent.** Attaching `/executive-readout` in the Analyst agent's chat gives the toast *"Using agents and skills together is not supported yet. Please use one at a time."* The working route: ask the agent for a findings summary → the response's copy button → new chat → `/executive-readout based on the following:` + paste. That is three extra steps in the 6-minute Marketer leg, and it teaches that Skills live in the assistant, not in agents. Alternatives: (a) the §5 fallback (verified query + glossary term on the data agent); (b) put the readout format in the data agent's own instructions, so "give me the executive readout" works in one step. Planning call.
11. **S3 → SC1: the dictionary's cohort verified query does not reproduce the story's pivot number.** Asked for the cohort's conversion change, the agent reported lapsed Compass cold-market conversion *"fell from 3.86% to 3.50%"*. The walkthrough's pivot is **6.74% → 2.43%**. That figure is on **warm-escape browsing sessions** (`WHERE s.viewed_warm_escape`), August 2025 vs. August 2026. The verified query *"Conversion rate by customer cohort and month"* in `data/docs/data-dictionary.md` has no `viewed_warm_escape` filter, so it measures all sessions month by month. **Fix:** replace it with the walkthrough's query (anomaly-walkthrough.md, the cohort table under "lapsed Compass member, cold market | 5,546 | 6.74"), or add `WHERE s.viewed_warm_escape`. Add to the instructions or glossary: *"cohort conversion means conversion on sessions that viewed a warm escape, compared with the same month last year."* Other upstream errors also flowed into the readout: the incident at "15 to 25 bookings" (walkthrough ≈65), the CMP-002 baseline at "$12.4k to $14.9k per week" (walkthrough ≈$20.9k; $12.4k is the transition week), weekly conversions "63 → 1–4" (walkthrough ≈100 → 3–9), and revenue −$1.59M / −18.9% (walkthrough −$1.62M / −19.3%).
12. **S3 — a skill generated from a one-line request is generic and invents facts.** "Create skill with Gemini" turned the one-line request into a long incident-post-mortem skill (engineering examples, "SLA impact"). The readout it produced opened with a preamble ("Good call synthesizing this…"), used an emoji heading, bold and rules, and stated a remediation as done: *"engineering has deployed patches to prevent session data loss"*. That is not in the data. If the Skill route stays, ship a Cymbal-specific instruction block (or a SKILL.md for **Upload skill**) that says: output only the three paragraphs, with no preamble, headings, emoji or bold; use only numbers present in the input; paragraph 3 gives *recommendations*, never claims that actions were taken.
13. **S4 → SC3 decision (Patrick): Cloud Run services deploy with `--min-instances=1`.** Cold starts are slow enough to break agents and connectors that call them. This applies to every lab service (Toolbox, the orchestrator, any FastMCP tool).
14. **S4 → SC3/Task 3: the custom MCP data store needs an identity provider on the Gemini Enterprise instance first.** The create wizard's Configuration step blocks with *"You must configure your access control settings before you continue"* (**Reload** / **Configure access control**). The fix: Gemini Enterprise → Settings → **Authentication** → the global location showed *"Identity provider not set up"* → set to **Google Identity** → back to the wizard → **Reload** → **Create** is enabled. Same as earlier GE labs. Either SC3 sets it at Start Lab (if scriptable), or it becomes a Task 0 / Task 3 Builder step.
15. **S4 — the checklist card was revised** by the planning conversation before it worked (the version used is quoted under S4: Route A Toolbox, Route B FastMCP stub, then the data-store steps). The run so far used **Route B**.
16. **S4 → planning: Task 3's "read the tools' schemas" beat has no home in the actions list.** Gemini Enterprise's Actions tab shows only Name / Type / Status. Where the contract *is* visible: (a) the **Review: Activate Segment** confirmation card (labelled, editable parameters, a great Marketer moment); (b) the `tools.yaml` itself (Builder: open it in the Cloud Run service's secret, or in the repo); (c) `docs/services.md`. The Builder leg needs rewording.
17. **S6 → planning: Workflow vs. Chat agent for Brand Studio. Follow-ups *do* work in a workflow, contrary to the docs.** The builder offers two agent types (New agent → Chat agent | Workflow), and the docs say workflows don't support multi-turn conversation. But in practice, a follow-up in the same chat after a workflow run (*"That's too stiff for us. Make the Insight section warmer, and take out the guaranteed lowest price line."*) got a revised brief. Missing inputs are also gathered by chat, not a form. So a Workflow can carry Task 4's push-back beats, and it gives the Builder a visible canvas (Manual Trigger → Gemini agent → MCP step) that a Chat agent lacks. Risk: this contradicts the documented behaviour and may change (R4). Planning call; the plan's D8 wording ("Workflow Builder") can stand if the workflow route is chosen. **Settled by S7: Chat agent.** A Workflow agent errored on image generation, and a Chat agent generated images fine. Original reasoning: Nothing in Tasks 4–5 needs a Workflow's triggers, schedules or approval steps; the conversation is the point; and relying on undocumented follow-up behaviour is a risk. The one argument for a Workflow is **enforced ordering**: the variant-performance lookup as a fixed step before generation (Task 5). If the instruction-based ordering proves unreliable in a Chat agent, fix it in the tool or the instructions. Mention enforced ordering as a one-line aside.
18. **S5 → SC2/SC3: agent deploy gotchas.** (a) Gemini 3.x models return 404 in `us-central1`; every ADK agent needs `GOOGLE_CLOUD_LOCATION=global` (env var on Cloud Run; set in code before imports for Agent Runtime). (b) `adk deploy cloud_run --a2a` serves no agent card without an `agent.json` in the agent folder. (c) Gemini Enterprise's A2A registration rejects the generated card until `supportedInterfaces` and `preferredTransport` are removed. Check the trimmed card into the repo, and have provisioning or the lab text paste that version.
19. **S8 → SC3: start the corpus import first.** It takes about 10 min with the Layout Parser. Kick it off at the top of the startup script, in parallel with the BigQuery load and the Cloud Run deploys.

---

## S1 — Fresh project, Gemini Enterprise activation, toggles

**Status: PASS** (2026-09-18)

- Data load: `sql/load.sh` + model started first, as the checklist says. It needed `chmod +x` first (see Contradiction 3), then ran. _Load time: not recorded._
- Gemini Enterprise trial license activated; app created as **test-app** (display name "Test App").
- **Configurations → Feature management**, toggles turned on exactly as the UI names them: **Enable chat agents**, **Enable workflow**, **Enable model selector**, **Enable canvas**, **Enable skills**, **Enable Gemini 3.8 Flash**. Saving shows the toast *"Feature controls updated successfully. It [may take some time] for the changes to take effect"*. Task 0 needs to tell students to expect that short propagation delay.
- The preview (end-user app) shows **Agents** with a new-agent button, and **Skills**. There is no path in the end-user app for adding a custom (A2A or Agent Runtime) agent.
- Admin left nav (the "Apps → test-app" breadcrumb): Overview, Connected data stores, **Actions**, Prompt chips, Configurations, **Agents**, **Skills**, Security, Integration. Links further down: Gemini Notebook, CodeAssist, Manage subscriptions.
- Agents page filters: **All / Google-made / Our agents**, plus **+ Add agent**. The agents table has Display name, Agent ID and Agent type columns; Core Assistant, My Agent and My Workflow (both *Employee-made*) and Deep Research (*Google-made (Deep Research)*) are listed. There is also an info banner: observability and user-content logging are off by default, with a **Configure agent logging** button.
- **+ Add agent → "Choose an agent type"** offers five cards: **Custom agent via Agent Runtime**, **Custom agent via Dialogflow**, **Custom agent via A2A** ("Custom Agents deployed anywhere that speak A2A protocol"), **Agents via Marketplace**, **Agents from Agent Registry** ("Agents in a registry within the Agent Gateway configured for this Gemini Enterprise instance").

**Locks:** Task 0 as written (with the toggle names). Both registration routes the plan needs exist in a lab project: A2A by card (the Task 1 data agent, the Task 3 fallback, the Task 6 orchestrator) and Agent Runtime (the S5 comparison). *Agents from Agent Registry* is new relative to §3. It is the Agent Gateway route, which could make the Task 6 governance aside concrete ("this is where a governed agent would come from; ours came by card"). Not load-bearing.

**Still open:** Skills was on in a fresh lab project with no allowlist step, so R5 is closed.

## S2 — BigQuery data agent → Gemini Enterprise via A2A + OAuth

**Status: PASS** (2026-09-18). The primary path works end to end in a Qwiklabs project. See Contradictions 4–6 for what the lab and data must absorb.

**APIs.** BigQuery, Gemini Data Analytics and Gemini for Google Cloud enabled. "Knowledge Catalog" was not found by that name; not needed.

**UI path and names.** BigQuery → left nav **Agents** (URL `/bigquery/agents_hub`) → the hub reads *"Chat with your data · Built with Gemini"*, with tabs **Agents / Knowledge sources** and side links **New chat · Agent catalog · Monitoring (Preview)**. **+ New agent** opens the **Editor**: *Agent name* (required), *Agent description*, **Knowledge sources → Add source** → a **Select sources** dialog (tabs All / Recent, a *Search BigQuery resources* box, a *Show starred only* toggle, then a project → `cymbal_voyages` → table tree with checkboxes).

**Lab-writing notes from the build:**
- **The table list scrolls, and the fact is hidden.** The picker shows about 8 tables and then what looks like empty space, so it looks as if tables are missing. The step must list the tables to tick in the picker's own (alphabetical) order and say how many there are. Patrick selected **9**, confirmed as, in picker order: `ad_performance`, `bookings`, `campaign_history`, `customer_month_status`, `customers`, `destinations`, `packages`, `plan`, `web_sessions`.
- **No auto-save and no leave warning.** Clicking away from the editor discards the agent silently. Tell students to click **Save** as soon as the sources, instructions and glossary are in, before testing.
- **Glossary terms and verified queries are slow to enter by hand** (Contradiction 5).
- **Verified query UI:** the top field is labelled **Question** and the SQL below it is the answer. Our verified-query titles are phrased as labels ("Bookings versus plan by month and category"). Reword them as questions, e.g. "How did bookings compare with plan by month and category?", "How did conversion change by customer cohort and month?", "How did paid spend move by campaign each week?"
- **Answers can be downloaded as PDF** (download button on each answer). This could be useful as the Task 2 readout artifact.

**Answers (same question: "Why did warm escapes bookings miss plan in August?")**
1. *Unaided* (tables only; column/table descriptions present from `load.sh`): the whole story in one answer, including the CMP-002 → CMP-012 rotation on Jul 24. Its summary table (cold 1,981 plan / 1,318 actual / −663 / −33.5%; mild −23 / −1.7%; warm −15 / −2.8%) matches the anomaly walkthrough exactly. **Too good; see Contradiction 4.**
2. *With instructions, glossary and verified query* (BigQuery Studio): cold markets −33.5% (1,318 vs. 1,981), a split by active vs. lapsed Compass members from `customer_month_status`, cold revenue $2.83M vs. $4.29M planned (−34.2%). It does **not** jump to the campaign cause. The instruction "break it down … before speculating" makes it decompose rather than conclude. That is the behavior Task 2 wants.
3. *Inside Gemini Enterprise via A2A:* trace shows *Analyzing context → Matched a verified query → Context retrieved → Running a query ×2*. It answered by market (Chicago −153 / −34.9%, Toronto −119, Boston −103 … all eight cold markets −28% to −35%; mild and warm within about ±8%). The per-market gaps sum to −701, and the cold markets to −663: consistent with the walkthrough. Output was a table plus bullet findings. _Charts: not observed (R3 still open)._

**Publishing.** You publish **twice**. The first publish offers registration with **Agent Registry**. The second offers **Integrate via A2A** with **Copy JSON** (the agent card). Agent Registry needs an Agent Gateway configured first, so it is not viable for the lab. The A2A JSON path was used: Gemini Enterprise admin → Agents → + Add agent → **Custom agent via A2A** → paste.

**First use in Gemini Enterprise.** The first question produced a prompt that the agent needs extra permissions, with an **Authorize** button → a "Sign in with Google" page → signed in as the student → the query ran. One authorization per user, as §3 predicted.

**Re-test after the data fix (Sep 18, new agent, same 9 tables, no instructions, same question):** the leak is closed. The agent no longer names CMP-002 → CMP-012, a budget rotation, or July 24 as the cause. Its answer:
- Headline: *"…primarily due to severe underperformance in key cold-weather feeder markets (e.g., Chicago, Toronto, and Boston, each trailing plan by 30% to 35%), driven by price increases on core packages (Aruba Classic Week and Maui Classic Week took effect August 1, 2026) alongside a sharp pullback in dedicated warm-escape retargeting ad spend."*
- Per-market table identical to the earlier runs (Chicago 439/286/−153 … Milwaukee 106/71/−35).
- Drivers it lists: (1) the cold-market deficits (correct); (2) **the Aug 1 price increases**: Aruba Classic Week $1,449 → $1,619, Maui Classic Week $1,319 → $1,479, *"dampening early-booking conversion"* (the planted red herring, stated as a cause); (3) *"Paid media spend was heavily directed toward Fall City Breaks and generic Brand Search, while dedicated Warm Escapes Retargeting received only ~$4.6k in media spend during August"*. That matches the $150/day keep-alive × 31 days, so it is found from the spend data, not from text.
- **What Task 2 still has to do:** rule out the price increase (≈18 bookings, 3% of the gap), rule out the Aug 9 incident, establish **when** retargeting fell and from what level (Jul 24; $3,000/day before), and connect it to the mechanism: lapsed Compass members in cold markets still browsing but converting at 2.43% instead of 6.74%. The unaided agent did none of that. The thread is intact.
- Caveat: answers are non-deterministic. Another run may weight the drivers differently, so the lab text should not quote the unaided answer verbatim.

**Timings:** _not recorded._ Builder effort was dominated by entering the glossary and the verified query.

**Locks:** Tasks 1–2 primary path (no ADK analyst fallback needed). Q2: end-user OAuth works in a lab project. **Confirmed:** publishing handled the OAuth client and consent screen. Patrick created nothing by hand, so there is no OAuth step for SC3 or the students. R3 is still open.

## S3 — Assistant Skill wrapping the readout

**Status: PARTIAL** (2026-09-18). A Skill can be created and invoked in the assistant. It cannot be combined with an agent, so the plan's "hand off to the registered data agent" does not work (Contradiction 10).

**UI path and names.** Web app → **Skills**. Empty state: *"Add your first skill — Create with Gemini, explore the marketplace, or upload your own to get started"*, with three buttons: **Create skill with Gemini**, **Browse skills**, **Upload skill**. *Create skill with Gemini* opens a split view: a chat box on the left, and on the right a form with **Name\*** (64 chars), **Description\*** (1,024), *Created by: You*, **Trigger** (auto-filled as `/` + the slugged name), **Instructions** (500,000), **Cancel / Save**.

**What was done.** Typed into the left-hand chat: *"Name: Executive readout. Instructions: a three-paragraph narrative (what happened, why, what we'll do), numbers inline, no charts, no bullets, one sentence on the business impact."* Gemini filled the form: name `executive-readout`, trigger `/executive-readout`, a 345-character description ("Synthesizes complex operational updates, project milestones, or incident reports into a concise, professional, three-paragraph narrative executive…"), and long instructions (Overview, When to Use, Expected Input/Output, Required Tools/Data, a four-step workflow including a self-check). The Instructions field **renders the Markdown**. There is no raw or source view, and copying out of it drops the formatting. The lab should hand students text to paste or a file to upload, never ask them to edit the generated Markdown.

**Invocation.**
- In the Analyst agent's chat, adding the skill chip gives the toast *"Using agents and skills together is not supported yet. Please use one at a time."*
- Workaround that worked: in the agent, prompt *"Summarize what we found about why warm escapes missed plan in August 2026: the size of the miss versus plan, where it was concentrated by market climate, which customer cohort's conversion changed and by how much, what happened to the Warm Escapes Retargeting campaign and when, and what the site incident and the price change contributed. Include the numbers."* → copy the response → **new chat** → `/executive-readout based on the following:` + paste.

**Output.** Three narrative paragraphs with numbers inline, a bolded business-impact sentence and no bullets, so the format is honoured. But: a chatty preamble and sign-off, an emoji heading, horizontal rules, one invented action, and the upstream numeric errors (Contradictions 11–12). Right: 701 / 3,175 / 3,876 / −18.1%; cold −663 / −33.5%; mild −1.7%, warm −2.8%; CMP-002 collapse "starting in late July". Wrong or unverified: see Contradiction 11. The Aug 1 price change is described as a compounding cause, not ruled out (about 3% of the gap).

**Timings:** _not recorded._

**Locks:** Skills are assistant-only (confirms §2 and §3). Task 2's Builder leg as written ("a Skill … [that] produces the executive readout" from the data agent's findings) needs either the copy/paste hand-off or a different route; see Contradiction 10. **Confirmed:** it must be a new chat. Choosing an agent opens a dedicated agent conversation, and there is no way to leave or deactivate the agent mid-conversation. The hand-off is always copy → new chat → paste.

**Draft Cymbal-specific skill:** `skills/executive-readout/SKILL.md` (Sep 18). Its frontmatter `name` / `description` and its body map onto the form's Name, Description and Instructions fields, so students can paste three fields instead of uploading a file. **Re-test with the Cymbal skill (Sep 18):** Patrick edited the existing skill in place and pasted the new text. Editing works; no upload is needed. The prompt was *"Please give me the /executive-readout for the following:"* + the Analyst agent's summary (copy button). The copy **flattens tables**: the climate table arrived as one run-together line (`climatebookingsplanned_bookings…`), and the skill still read it correctly. Output: three clean paragraphs, no preamble, headings, emoji or bold; recommendations written as proposals ("We recommend that marketing…", "product and pricing teams should review…"); no invented actions; every number traceable to the input. Two small misses, both fixed in the SKILL.md since: a `---` separator plus a spurious *"Missing from the findings: The specific business impact of the recommendations"* (the impact sentence is now defined as the impact of the miss itself), and no separator line allowed. **The remaining errors are all upstream in the agent's summary:** it dated the CMP-002 collapse **August 1** (monthly totals $69,944 → $4,650; true date Jul 24), used the 3.86% → 3.50% cohort figure, left the incident unquantified, and kept the price change as a contributing cause. The skill faithfully passed these on. Fix prompt for SC1: `docs/sc1-fix-prompt-2.md`.

**Form and edit paths (confirmed Sep 18):** the right-hand form in *Create skill with Gemini* is directly editable, so students can ignore the Gemini chat and paste Name, Description and Instructions. An existing skill opens at **Skills → *skill name*** as a detail page: title, `/trigger`, description, *Created by*, the instructions in a panel with a **rendered / source (`<>`) toggle**. The create/edit form (with the Gemini chat on the left) has no such toggle, so raw Markdown can be seen only on the detail page, and a **New chat** button. The **⋮** menu has **Edit / Disable / Delete**; *Edit* reopens the three-field form. In the Skills list the skill appears under **Enabled** as a folder containing `SKILL.md`. That confirms the SKILL.md packaging, so **Upload skill** is a possible alternative, though no file upload is needed. **Confirmed:** the skill page's **New chat** button opens a new chat with the skill's trigger already entered as the first thing in the message box. So the Task 2 hand-off is: copy the Analyst's summary → Skills → executive-readout → **New chat** → paste → send. Patrick has updated the skill in the spike project to the current `SKILL.md`.

## S4 — Custom MCP data store (Toolbox / FastMCP on Cloud Run)

**Status: PASS** (2026-09-18), via Route A (Toolbox).

**Server.** **Route B**, the minimal FastMCP stub from the checklist (`server.py` with a stub `resolve_segment` marked `readOnlyHint` and an `activate_segment` without it; `python:3.12-slim` + `pip install fastmcp`; `gcloud run deploy mcp-spike --source . --region us-central1 --no-allow-unauthenticated`; `roles/run.invoker` for `service-PN@gcp-sa-discoveryengine.iam.gserviceaccount.com`). Endpoint `https://mcp-spike-a76r57qoea-uc.a.run.app/mcp`. **Route A (MCP Toolbox for Databases with a `bigquery-sql` `resolve_segment` against `customer_features`) has not been connected yet.** It is the path Phase 2 builds on, so it still has to be run.

**Creating the data store.** Gemini Enterprise → Connected data stores → Create data store. Wizard: **Source → Data → Configuration**. Auth chosen: **None**. On *Configure your data connector*: Multi-region **global (Global)** (locked), **Data connector name** (the ID is derived from it and "cannot be changed later", e.g. `mcp-to-toolbox-example_1789768826082`), **Tag (optional)**, **Sensitive data protection policy** (optional), then the access-control block (Contradiction 14). Once the identity provider was set: toast *"You have successfully created a data connector"*.

**Data store page.** Breadcrumb Apps › test-app › Data stores › *name*. Left nav **Data / Actions (New)**. Banner: *"Actions for this custom MCP server are disabled. Go to the Actions tab to enable the actions you want available in Gemini Enterprise."* (**Go to actions**). Header links **Re-authenticate · View/edit parameters · View logs**. Details: Collection ID, Type `custom_mcp`, Region global, Instance URL, Connected apps `test-app`, Connector state **Active**, Content policy N/A.

**Actions.** The Actions tab was empty at first. After a short wait and **reload custom actions**, two actions appeared, displayed as **Activate segment** and **Resolve segment** (humanized from `activate_segment` and `resolve_segment`). Both are disabled by default, as §3 said. Enabled **Resolve segment** only.

**Assistant call (checklist step 5).** It called the tool and answered: 4,210 members, average propensity 0.31, 3,644 email-contactable (86.6%), "Inactivity threshold ≥ 180 days". These are **Route B's hard-coded stub values** (`size 4210, avg_propensity 0.31, email_contactable 3644, lapsed_days_min = 180`), so this was not a data problem. What it does show: Gemini presents stub output **confidently, as fact**, dressing the default parameter up as a "standard lapsed definition". It also formats in its usual Markdown (emoji headings, a table, bullets). _No confirmation prompt was reported before the read-only call; confirm._

**Route A re-run (Sep 18).** The card's first `tools.yaml` block used an unquoted `<<EOF`, so Cloud Shell's command substitution likely stripped the backticked table names. It was replaced with `services/toolbox/spike/tools.yaml`: the current `kind: source` / `kind: tool` format, a marketer-worded `resolve_segment` with `annotations: readOnlyHint: true`, and an `activate_segment` that MERGEs a deterministic receipt (`act-` + MD5 of description|channel) into `activations` and returns it (`readOnlyHint: false`, `idempotentHint: true`). Secret updated; `toolbox-sa` given `roles/bigquery.dataEditor`; `gcloud run services update toolbox --min-instances=1`. New data store **"MCP Server CR Test"**; all other connectors disabled.
- **Actions tab:** a table with Name / Type / Status and checkboxes; bulk **Enable actions / Disable actions**. Names are shown humanized: **Activate Segment**, **Resolve Segment**. Type **Custom**, Status **Disabled** by default. **The actions list does not show parameters or descriptions**, so the plan's "read the two tools' schemas in the actions list" beat has no UI there (see Contradiction 16).
- **Resolve:** *"How big is the audience of lapsed Compass members in cold-weather markets who browsed warm destinations…"* → **3,838 customers, avg propensity 0.156, 8.7% ≥ 0.30 (334), 3,271 email-contactable (85.2%)**. Exact match with the walkthrough (reference scores; BQML re-scoring not re-run after the reload). The tool description's wording came back in the answer ("no booking in 12+ months", "0 to 1 scale", "within 60 days"). _No confirmation prompt reported for the read-only call; confirm._
- **Activate:** *"Activate that audience to email."* → a **"Review: Activate Segment"** card with editable, labelled fields **Channel\*** (email), **Audience Size\*** (3838), **Segment Description\*** ("lapsed Compass members in cold-weather markets who browsed warm destinations"), plus **Send / Cancel**. `readOnlyHint: false` gives the confirmation, as designed. **This card is where the tool contract becomes visible to the Marketer**: parameter names humanized, values filled in by Gemini and editable.
- **Send → receipt:** `act-caa4caf2510b`, status Accepted, channel email, 3,838, submitted Sep 18 2026 5:23 PM local. **Toolbox `bigquery-sql` runs a multi-statement script (DECLARE + MERGE + SELECT) and returns the final SELECT's rows**, so the activation receipt needs no custom server. Gemini then offered next steps (draft email copy, run projections). - **Retry (same chat, same request, Send again):** Gemini replied that the audience *"is already fully activated on the email channel… the existing active receipt has been retrieved"*, with the same `act-caa4caf2510b` and the original timestamp. `activations` holds **one row**. The deterministic receipt key held because Gemini reused the identical segment description. Phase 2 should still key on something Gemini can't reword (see open items).

**Locks:** Task 3 primary path. Phase 2 services: `resolve_segment`, `activate_segment` and (by extension) `variant_performance` as Toolbox `bigquery-sql` tools in one `tools.yaml`, with Toolbox run from Google's image (pin a version, not `:latest`), the file mounted from Secret Manager, `--min-instances=1`, and `run.invoker` for the Discovery Engine service agent. The A2A fallback (S5) is not needed for Task 3.

**Open items for Phase 2:** the `idempotentHint` receipt key currently hashes the free-text description, so make it robust to rewording (e.g. hash the resolved filter values); the real `resolve_segment` needs the refinements Task 3 walks through (email-only, lapsed 12–24 vs. 24+ months, widen to all members, drop recent bookers: walkthrough variants A–F) plus the semantic destination match; timings for the Toolbox deploy and for the actions to appear were not recorded.

**Org policy (Q1): closed.** Both custom MCP data stores (`mcp-spike` and `toolbox`, auth None) were created with **no org-policy error**, no override and no FQDN allow-listing in a Qwiklabs project. The only prerequisite was the identity provider (Contradiction 14).

## S5 — ADK agent via A2A: Cloud Run, then Agent Runtime

**Status: PASS** (2026-09-18; run earlier in the session, reported late).

- **Cloud Run (A2A):** agent **`orchestrator`**, registered by agent card (Custom agent via A2A). Appears in the `@` list.
- **Agent Runtime:** agent **`orchestrator2`**, registered via Custom agent via Agent Runtime. Appears in the `@` list.
- **Agent Runtime (`orchestrator2`):** `adk deploy agent_engine`, **2 min 42 s** (`real 2m42.124s`). Needed `os.environ["GOOGLE_CLOUD_LOCATION"] = "global"` at the top of `agent.py`. Registered via **Custom agent via Agent Runtime**. The test chat answered, but returned `send_offer` for the C000123 case instead of the expected `route_to_loyalty_team`. The likely cause is a tier-case mismatch in the stub tool, or the model not calling the tool; not a platform failure.
- **Cloud Run (`orchestrator`, service `orchestrator-a2a`):** `adk deploy cloud_run --a2a` from source, **2 min 25 s** (`real 2m25.848s`). **Gotchas:** (1) no agent card is served until an `agent.json` is added to the agent folder; (2) the card is at `/a2a/orchestrator/.well-known/agent-card.json`; (3) Gemini Enterprise's *Custom agent via A2A* rejected the generated card until `supportedInterfaces` and `preferredTransport` were **removed**; (4) the first chat failed with a **404 on `gemini-3.5-flash` in `us-central1`**, fixed with `gcloud run services update orchestrator-a2a --update-env-vars GOOGLE_CLOUD_LOCATION=global`. It then answered correctly: route to loyalty team, with a reason.
- **R1 closed:** Agent Runtime deploys in under 3 minutes, well inside the ~8-minute threshold, so both hosts fit Start Lab.
- **Hosting (planning call; the companion leans Cloud Run):** Cloud Run keeps every lab service on one pattern (prebuilt image from Artifact Registry, `--min-instances=1`, `run.invoker` for the Discovery Engine agent, registration by pasting a card, the same move as Task 1's data agent) and gave the correct decision in the test. Agent Runtime deploys from source (no prebuilt image) and adds a second registration path to teach. Either way: set `GOOGLE_CLOUD_LOCATION=global` (the Gemini 3.x models are served from `global`), and check in `agent.json` plus the trimmed card.

## S6 — Workflow Builder agent on a GCS data store

**Status: PASS** (2026-09-18, marked by Patrick). **Open items, not tested:** (1) the tightened banned-phrase + template instruction and its re-run; (2) a **Chat agent** build: the same grounding, whether the builder offers the brand corpus and the Toolbox MCP data store as tools, and the Task 4 push-back beats; (3) adding an MCP step or tool to the agent; (4) **Turn on** and the share flow for a pair. Fold these into the S7 session or the Mon Sep 21 end-to-end walk.

**Data store:** **Cymbal Voyages Brand Corpus**, a Cloud Storage source pointed directly at `gs://class-demo/cymbal-voyages/v1/brand_corpus/` (no copy into a project bucket), with the **Layout Parser** (7 of the 12 PDFs rely on tables, including the legal claims list). _Import time and any permission issue: see S8._

**Two kinds of agent.** Web app sidebar → **New agent ›** → **Chat agent** | **Workflow**. Per the docs (workflow-builder/workflow-agents), a **Workflow** runs a sequence of steps from a trigger (Manual via `@name` with input fields, Schedule, or Event/App event (Preview)). It **does not support multi-turn conversation**: the user supplies inputs once at the start. A **Chat agent** is conversational.

**Workflow build (what Patrick saw).** "Let's build your workflow" page: a prompt box plus a **connectors** picker (*Enable all connectors*, Google Search, and each data store: **Cymbal Voyages Brand Corpus**, **MCP Server CR Test**, **MCP To Toolbox Example**, each with a toggle). So MCP data stores are selectable as workflow tools. Prompt: *"Create a Brand Studio that writes campaign briefs in the house voice and never uses a phrase on the legal list."* with the corpus on. Result: workflow **"Brand Studio Brief Writer" (Draft)**. Canvas: **Manual Trigger → Brand Brief Writer** (a Gemini agent node) → **+**. Top bar: **Edit / Test**, version history, share, **Turn on** ▾. Left panel **Changes made** with **Diff / Undo** and a "What would you like to change?" box. Right panel for the trigger: **Trigger type** (Manual ✓, App event *Preview*, Schedule) and **Input fields**, auto-created as `key_messaging_requirements`, `target_audience`, `campaign_name`, `campaign_objectives` (+ Add field). For the agent node: **Instructions** (numbered steps; input values appear as chips like *Manual Trigger: campai…*, with a `</>` source toggle and **+**), **Connected apps**, **Files (0)**. The generated instructions: search the corpus for voice guidelines, tone, style rules and the legal "phrase exclusion list"; review the campaign parameters; draft the brief.

**Invoking the workflow.** The draft workflow doesn't appear in the `@` list until you start typing its name; then it appears and becomes a chip ("Brand Studio Brief Writer"). The `@` list also shows Deep Research, Cymbal Voyages Analyst, orchestrator, orchestrator2 and My Agent. There is **no form**. Sending just "Start the workflow" makes it ask in chat: *"What is the name or theme of the campaign, what are its key objectives, what are the key messaging requirements, and who is the target audience?"* Sending the values as labelled lines (`campaign_name: …`, `target_audience: …`, `campaign_objectives: …`, `key_messaging_requirements: …`) in one message worked. Plain-English labels would probably work too (untested).

**Test run (Fall Recovery: Warm Escapes, 3,838 lapsed Compass cold-market browsers, with "Guaranteed lowest price on every trip" planted in key messaging):**
- **Grounding: PASS.** It cited *Winter Sun Early Bird (CMP-003)*: "bonus points are a more effective incentive than discounts for this audience, as they are perceived as a form of recognition". It chose **beach couple** imagery and **"Plan your escape"** as the CTA, matching the corpus retrospective and the creative-variants learning for `lapsed_compass_cold`.
- **Banned phrase: FAIL.** "guaranteed lowest price" is the **first row** of the legal list's banned table (07-legal-claims-list.md, with the approved alternative "From $X per person, taxes and fees included."; the list also bans paraphrases). The brief **kept the line** in Key Messaging, and under Legal Review Notes said only that it *"is not on the approved list of legal claims and will require a legal review."* The instruction Gemini generated ("strictly excluding prohibited phrases") lost to the user's explicit input. **Lab consequence (a good one):** the Builder's policy line has to be explicit, e.g. *"If a requested phrase or its paraphrase is on the legal list's banned table, do not use it. Say which rule it breaks and use the approved alternative from the same row."* Worth making the Task 4 beat: first run fails, the Builder tightens the rule, re-run.
- **Template adherence: PARTIAL.** The template has ten headings (Objective, Audience, Insight, Channels, Offer, **Budget and modeled reach**, **KPIs**, Creative direction, Legal review notes, Retrospective). The brief skipped Budget/modeled reach and KPIs, and added a "Key Messaging Requirements" section. The instructions should say "use exactly the template's headings, in order; leave Retrospective as 'To be added after the campaign'". Note: plan D6 says the template has nine headings; the file has ten.
- **Follow-up in the same chat: WORKS.** The push-back above produced two acknowledgements (*"No problem. I'll rewrite…"* then *"Of course. Here is the revised brief…"*; a double-reply quirk) and a rewritten brief with a warmer Insight and the price line removed. Iteration is possible despite the docs (Contradiction 17).

## S7 — Image generation

**Status: PASS** (2026-09-18), via a Chat agent.

- **Assistant: PASS.** It generated a photoreal hero image: a couple walking hand in hand along the water's edge at golden hour, with no text in the image, which is what was asked. _Model shown and exact prompt: to record._ **On-brief check against `12-creative-production-standards.md`:** the *Beach couple* row says "two people at the water's edge or on a terrace, mid-conversation, no posing. Warm light, real skin, **no sunset silhouettes**", and is "the lead style for every win-back campaign". The image mostly fits (water's edge, warm light, not a silhouette). But the setting reads as a temperate coast (dune grass, driftwood, headlands) rather than a Caribbean/Mexico/Hawaii warm escape, and it is a posed-looking back-view walk. That makes a good Task 5 critique beat ("check it against the production standards"), and a Skill or agent instruction carrying the standards should fix it.
- **Workflow agent (Brand Studio Brief Writer): FAIL.** Image requests *"were throwing errors"*. _Exact error text: to record._ This confirms R2 for the Workflow route.
- **Chat agent: PASS.** Patrick built a new Brand Studio as a **Chat agent**, and image generation *"worked fine"* there. _Model name, whether the corpus grounding and the Task 4 beats behave as in the Workflow, and whether the Toolbox data store is offered as a tool: to record on the Sep 21 walk._
- **MCP tool returning an image URL:** not tested.
- **Banned phrase in image text** ("Guaranteed lowest price" on the image): not tested.

**Locks:** Brand Studio is a **Chat agent**, grounded on the brand corpus and generating images itself, so Tasks 4 and 5 stay in one agent. R2 is resolved for Chat agents (it holds for Workflows). No image-generation tool or FastMCP server is needed. **Confirmed:** the Chat agent builder's tools picker (the **+** in the instructions box) lists **Select All**, **Cymbal Voyages Brand Corpus**, **Google Search**, **MCP Server CR Test** and **MCP To Toolbox Example**, each with a toggle. So the Toolbox MCP data store can be attached to Brand Studio. **Note for the lab text: Google Search is ON by default and the brand corpus is OFF.** The Builder step must say to turn the corpus on and **Google Search off**. With web search on, answers can pull in real competitor brands and prices, which breaks the "no real brands" rule and blurs the grounding lesson. Still to confirm on the Sep 21 walk: `variant_performance` actually called by the Chat agent, so it "consults performance before it generates", and the banned-phrase-in-image test.

## S8 — GCS data store import time

**Status: PASS (tight)** (2026-09-18).

- **Cymbal Voyages Brand Corpus**, Cloud Storage source pointed **directly** at `gs://class-demo/cymbal-voyages/v1/brand_corpus/` (no copy into the project, and no permission error, so the lab project's Discovery Engine service agent could read the bucket), **Layout Parser**, 12 PDFs: **about 10 minutes** from Create to indexed (Patrick's estimate).
- **Start Lab consequence (SC3):** fire the data store creation and import **first**, in parallel with the BigQuery load (~5 min) and the Cloud Run deploys (~2–3 min each), so all of it lands inside the ≤15-min budget. Task 4, where Brand Studio first needs the corpus, is roughly an hour into the lab, so a slow import is not student-visible as long as it starts at Start Lab. The Digital Parser would likely import faster, but the legal list's tables need the Layout Parser (S6).
- Direct import from `class-demo` works, so **no copy step is needed** in provisioning. Treat `v1` as frozen once labs point at it (`staging.md`).
